#pragma once

#include <stdarg.h>
#include <cstddef>

namespace std {
	// May need to move this to stdio.h
	extern "C" int vsnprintf(char *buffer, size_t bufsz, const char *format, va_list vlist );
}
